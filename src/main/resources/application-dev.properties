spring.jpa.show-sql=true

spring.datasource.url=jdbc:postgresql://localhost:5432/${POSTGRES_DB:stock_analyzer_database}
spring.datasource.username=${POSTGRES_USER:stock_user}
spring.datasource.password=${POSTGRES_PASSWORD:change_me}